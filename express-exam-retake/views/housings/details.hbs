<main>
    <section id="deatils-page">
        <div class="wrapper">
            <div class="product-img">
                <img src="{{housing.homeImageUrl}}">
            </div>
            <div class="product-info">
                <div class="product-text">
                    <div class="product-text">
                        <h1>Notice: {{housing.notice}}</h1>
                        <h2>Type: {{housing.type}}</h2>
                        <h4>Year: {{housing.year}}</h4>
                        <h4>City: {{housing.city}}</h4>
                        <p>Description: {{housing.propertyDescription}}</p>

                        <!-- if there are already tenants of the housing, separate their names with a comma and a space ","  -->
                        {{#each housing.rentedHome}}
                            <p>People rented this housing: {{this.name}}</p>
                        {{else}}
                            <!-- If not display: -->
                            <p>People rented this housing: There are no tenants yet.</p>

                        {{/each}}


                    </div>
                </div>
                <!-- if there is no registered user, do not display buttons-->
                {{#if isAuthenticated}}
                    <div class="product-btn">
                        <!-- Only for registered user and creator of the housing offer-->
                        {{#if housing.isOwner}}
                            <a href="/housing/edit/{{housing._id}}" class="edit">Edit</a>
                            <a href="/housing/delete/{{housing._id}}" class="remove">Delete</a>
                        {{else}}
                            {{#if housing.housingLeft}}
                                <!-- logged in user with available pieces-->
                                {{#if housing.isRented}}
                                    <p class="alRentHome">You have already rent this home</p>
                                {{else}}
                                    <a href="/housing/rent/{{housing._id}}" class="rentHome">Rent a home, available 2 housing</a>
                                {{/if}}
                            {{else}}
                                <!-- logged in user with no available pieces-->
                                <p class="no-housing">No Housing Available!</p>
                            {{/if}}
                        {{/if}}
                    </div>
                {{/if}}

            </div>
        </div>
    </section>
</main>
